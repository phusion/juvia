<%= {
  :status    => 'ok',
  :action    => 'AddComment',
  :site_key  => @site_key,
  :topic_key => @topic_key,
  :html      => render(:partial => 'api/comment', :formats => [:html], :locals => { :comment => @comment })
}.to_json.html_safe %>
