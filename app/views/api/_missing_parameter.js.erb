<%= render :template => 'api/base', :formats => [:js] if @include_base %>
<%
  options = {
    :action    => 'ShowError',
    :container => @container,
    :site_key  => @site_key,
    :topic_key => @topic_key,
    :html      => render(:partial => 'missing_parameter', :formats => [:html], :locals => { :topic => @topic })
  }
  options[:css] = Juvia::Application.assets.find_asset('api.css').body if @include_css
%>
<%= juvia_handle_response(options) %>
