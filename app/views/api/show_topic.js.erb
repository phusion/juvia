<%= render :template => 'api/base.js' if @include_base %>
<%
	options = {
		:action    => 'LoadTopic',
		:container => @container,
		:html      => render(:partial => 'topic.html', :locals => { :topic => @topic })
	}
	options[:css] = render(:partial => 'stylesheet.css') if @include_css
%>
<%= juvia_handle_response(options) %>
