<h1>Listing admin_topics</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admin_topics.each do |admin_topic| %>
  <tr>
    <td><%= link_to 'Show', admin_topic %></td>
    <td><%= link_to 'Edit', edit_admin_topic_path(admin_topic) %></td>
    <td><%= link_to 'Destroy', admin_topic, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Topic', new_admin_topic_path %>
